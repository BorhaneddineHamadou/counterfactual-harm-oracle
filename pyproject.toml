[project]
name = "proxima"
version = "1.0.0"
description = "Counterfactual-harm test oracle for autonomous driving systems (replication package)"
requires-python = ">=3.9"
dependencies = ["numpy>=1.24", "scipy>=1.10", "scikit-learn>=1.3", "joblib>=1.2"]

[tool.setuptools]
packages = ["proxima", "adapters"]
